<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="self.he.mapper.UserMapper">
    <!--<insert id=" ">
   </insert>-->
    <!--<select id="" resultType="">
   </select>-->
    <select id="select" resultType="self.he.pojo.User">
           SELECT * FROM user
           WHERE name = #{name}
                 and pwd = #{pwd}
    </select>
</mapper>